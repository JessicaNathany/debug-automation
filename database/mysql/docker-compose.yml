version: '3.8'
services:
  mysql:
    image: mysql:5.7
    volumes:
      - ./init:/docker-entrypoint-initdb.d
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: cafedebug-mysql-local
      ports:
        - "3306:3306" 